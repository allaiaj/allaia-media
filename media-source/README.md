Compressed reel clips, kept out of public/ so they are NOT served.

Alloura's clip was the only self-hosted video on /brand-collabs, which
made that one card look different from the Instagram embeds around it.
Moved here so every card renders the same way.

To switch a card back to self-hosted video (autoplays muted on hover),
move the file to public/video/collabs/ and rebuild - the page picks it
up automatically. That only looks right once most cards have one.
